import * as React from 'react'
const styles = require('./nav.scss');

export const Nav = () => {

    return (
        <nav className={styles.nav}>
            <div className={styles.title}>Javascript task</div>
        </nav>
    )
};
